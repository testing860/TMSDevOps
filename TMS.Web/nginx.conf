# Backup using a temporary SQL tools container
sudo docker run --rm \
  --network container:sqlserver \
  mcr.microsoft.com/mssql-tools \
  /opt/mssql-tools/bin/sqlcmd \
  -S localhost \
  -U sa \
  -P "Test123!" \
  -Q "BACKUP DATABASE [TaskManagementSystem] TO DISK = N'/tmp/tms_backup_$(date +%Y%m%d_%H%M%S).bak' WITH FORMAT, INIT;"
^ Password in this DB: Test123!





events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    # Ensure CSS MIME type is explicitly set
    types {
        text/css css;
        application/javascript js;
        font/woff woff;
        font/woff2 woff2;
        application/font-woff woff;
        application/font-woff2 woff2;
    }
    
    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;

        # Blazor routing
        location / {
            try_files $uri $uri/ /index.html;
        }

        # Static files - SIMPLIFIED without complex if statements
        location ~* \.(css|js)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            types { text/css css; application/javascript js; }
            default_type text/css;
            try_files $uri =404;
        }
        
        location ~* \.(png|jpg|jpeg|gif|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            try_files $uri =404;
        }
        
        location ~* \.(woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            add_header Access-Control-Allow-Origin *;
            try_files $uri =404;
        }

        # API proxy - simplified without CORS headers (handle in API)
        location /api/ {
            proxy_pass http://api:5000/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}